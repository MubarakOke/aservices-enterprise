from django.urls import path, re_path, include
from .literals import APP_NAME, WORKER_REGISTRATION_VIEW, CUSTOMER_REGISTRATION_VIEW


app_name= APP_NAME

urlpatterns = [
    # path("register_customer",,name=CUSTOMER_REGISTRATION_VIEW),
    # path("register_worker",,name=WORKER_REGISTRATION_VIEW)
]